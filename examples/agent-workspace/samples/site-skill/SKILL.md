---
name: site-demo-form
description: Example-only workflow for the synthetic generic form fixture; copy and rename only after a user approves a real site's workflow.
---

# Synthetic form example — not an active production Skill

At the fixture's verified origin, `guide` shows entry `Save draft` and fields `Full name`, `Favorite color`, and `Notes`; use the returned current locators, not a remembered tab ID. For this tiny form, prefer the page Memory map alone. This file demonstrates the maximum useful shape of a site Skill, not a reason to promote it.

Stop at any login or CAPTCHA. Never submit or save without task-specific user authorization and a subsequent state check.
